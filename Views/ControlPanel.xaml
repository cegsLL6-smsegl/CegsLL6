<wpf:ControlPanel x:Class="CegsLL6.Views.ControlPanel"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:cegs="clr-namespace:AeonHacs.Components"
        xmlns:local="clr-namespace:CegsLL6.Views"
        xmlns:wpf="http://schemas.aeonhacs.com/wpf"
        mc:Ignorable="d">

    <wpf:ControlPanel.Resources>
        <wpf:TimeSpanToStringConverter x:Key="TimeSpanConverter" />
        <wpf:ProcessStateToVisibilityConverter x:Key="ProcessStateConverter" />
        <wpf:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
        <wpf:ToStringConverter x:Key="ToStringConverter" />
    </wpf:ControlPanel.Resources>

    <!--==============================================-->
    <!-- The display space -->
    <!--==============================================-->
    <Grid>
        <!-- Simplify references to Cegs properties -->
        <wpf:View x:Name="Cegs" Component="CegsLL6" Visibility="Collapsed" />

        <!-- System Uptime -->
        <StackPanel Margin="10" HorizontalAlignment="Left" VerticalAlignment="Top">
            <StackPanel>
                <Label Content="System Uptime" HorizontalAlignment="Center" Padding="0" />
                <Label x:Name="Uptime" AutomationProperties.HelpText="System Uptime"
                        Content="{Binding Component.Uptime, ElementName=Cegs, Converter={StaticResource TimeSpanConverter}, FallbackValue='100 23:59:59'}"
                        BorderBrush="{x:Static SystemColors.ActiveBorderBrush}" BorderThickness="0.5"
                        Padding="3,0,3,0" HorizontalAlignment="Center" />
            </StackPanel>
        </StackPanel>

        <!-- Process Control & State -->
        <Grid ColumnDefinitions="Auto,350,Auto" RowDefinitions="Auto,Auto,Auto,Auto"
              HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="10">
            <CheckBox Content="Auto-feed samples"
                      IsChecked="{Binding Component.AutoFeedEnabled, ElementName=Cegs}"
                      Grid.Row="0" Grid.Column="1" Margin="2"
                      HorizontalAlignment="Right" Visibility="Hidden"/>

            <!-- Process -->
            <Label x:Name="ProcessTime" AutomationProperties.HelpText="Process Run Time"
                   Content="{Binding Component.ProcessTime, ElementName=Cegs, 
                       Converter={StaticResource TimeSpanConverter}, 
                       TargetNullValue=0:00:00, FallbackValue=0:00:00}"
                   BorderBrush="{x:Static SystemColors.ActiveBorderBrush}" BorderThickness="0.5"
                   Grid.Row="1" Grid.Column="0" Margin="2" Padding="3,0"
                   HorizontalContentAlignment="Right" />
            <ComboBox x:Name="ProcessSelector" AutomationProperties.HelpText="Process"
                      IsEditable="False" SelectedIndex="0"
                      Grid.Row="1" Grid.Column="1" Margin="2" Padding="3,0" />
            <Button Content="Start" Click="StartButton_Click" AutomationProperties.HelpText="Start the selected process"
                    Grid.Row="1" Grid.Column="2" Margin="2" Padding="3,0"
                    Visibility="{Binding Component.NotBusy, ElementName=Cegs, Converter={StaticResource BoolToVisibilityConverter}, FallbackValue=Visible}" />

            <!-- Process Step -->
            <Label x:Name="ProcessStepTime" AutomationProperties.HelpText="Process Step Time"
                   Content="{Binding Component.ProcessStep.Latest.Elapsed, ElementName=Cegs, 
                       Converter={StaticResource TimeSpanConverter},
                       TargetNullValue=0:00:00, FallbackValue=0:00:00}"
                   BorderBrush="{x:Static SystemColors.ActiveBorderBrush}" BorderThickness="0.5"
                   Grid.Row="2" Grid.Column="0" Margin="2" Padding="3,0"
                   HorizontalContentAlignment="Right" />
            <Border AutomationProperties.HelpText="Process Step"
                    Background="Transparent"
                    BorderBrush="{x:Static SystemColors.ActiveBorderBrush}" BorderThickness="0.5"
                    Grid.Row="2" Grid.Column="1" Margin="2" Padding="3,0" >
                <TextBlock DataContext="{Binding Component.ProcessStep, ElementName=Cegs}"
                           Text="{Binding Latest.Description, TargetNullValue=''}"
                           wpf:View.Component="{Binding .}"
                           VerticalAlignment="Center"
                           TextTrimming="CharacterEllipsis" />
            </Border>

            <!-- Process Substep -->
            <Label x:Name="ProcessSubstepTime" AutomationProperties.HelpText="Process Substep Time"
                   Content="{Binding Component.ProcessSubStep.Latest.Elapsed, ElementName=Cegs, 
                       Converter={StaticResource TimeSpanConverter}, 
                       TargetNullValue=0:00:00, FallbackValue=0:00:00}"
                   BorderBrush="{x:Static SystemColors.ActiveBorderBrush}" BorderThickness="0.5"
                   Grid.Row="3" Grid.Column="0" Margin="2" Padding="3,0"
                   HorizontalContentAlignment="Right" />
            <Border AutomationProperties.HelpText="Process Substep"
                    Background="Transparent"
                    BorderBrush="{x:Static SystemColors.ActiveBorderBrush}" BorderThickness="0.5"
                    Grid.Row="3" Grid.Column="1" Margin="2" Padding="3,0" >
                <TextBlock DataContext="{Binding Component.ProcessSubStep, ElementName=Cegs}"
                           Text="{Binding Latest.Description, TargetNullValue=''}"
                           wpf:View.Component="{Binding .}"
                           VerticalAlignment="Center"
                           TextTrimming="CharacterEllipsis" />
            </Border>
        </Grid>

        <!-- Sample Info -->
        <StackPanel Orientation="Horizontal" AutomationProperties.HelpText="Active or Prior Sample"
                Background="Transparent" HorizontalAlignment="Left" VerticalAlignment="Bottom">
            <StackPanel.Style>
                <Style TargetType="{x:Type StackPanel}">
                    <Style.Triggers>
                        <DataTrigger
                                Binding="{Binding Component.Sample, ElementName=Cegs, Converter={StaticResource ToStringConverter}}"
                                Value="">
                            <Setter Property="Visibility" Value="Collapsed" />
                        </DataTrigger>
                        <DataTrigger
                                Binding="{Binding Component.Sample, ElementName=Cegs, Converter={StaticResource ToStringConverter}}"
                                Value="{x:Null}">
                            <Setter Property="Visibility" Value="Collapsed" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </StackPanel.Style>
            <TextBlock VerticalAlignment="Center" >
                <Run Text="Sample:" />
                <Run Text="{Binding Component.Sample, ElementName=Cegs, Converter={StaticResource ToStringConverter}}" />
            </TextBlock>
        </StackPanel>

        <DockPanel LastChildFill="True">

            <!-- LN and air section -->
            <local:LNAndAir DockPanel.Dock="Right" Margin="0,10,10,0" VerticalAlignment="Top"/>

            <!--==============================================-->
            <!-- Schematic Interface -->
            <!--==============================================-->
            <Canvas DockPanel.Dock="Left" Width="760" Height="350" Margin="0,10,0,80" VerticalAlignment="Top" HorizontalAlignment="Center">
                <local:VS Canvas.Left="223" Canvas.Top="154"/>
                <wpf:IM6 Canvas.Left="0" Canvas.Top="49" PressureSensorType="MKS_722_HP" />
                <wpf:CT Canvas.Left="267" Canvas.Top="124" PressureSensorType="MKS_722_LP"/>
                <wpf:VTT Canvas.Left="389" Canvas.Top="133" />
                <wpf:MC Canvas.Left="441" Canvas.Top="98" PressureSensorType="MKS_DMB" />
                <wpf:Split Data="M0,0 h21" Canvas.Left="493" Canvas.Top="133" />
                <wpf:GM6 Canvas.Left="532" Canvas.Top="49" PressureSensorType="MKS_722_HP" />

                <wpf:Valve Component="vIM_CT" Canvas.Left="249" Canvas.Top="128" />
                <wpf:Valve Component="vCT_VTT" Canvas.Left="371" Canvas.Top="128" />
                <wpf:Valve Component="vVTT_MC" Canvas.Left="423" Canvas.Top="128" />
                <wpf:Valve Component="vMC_Split" Canvas.Left="475" Canvas.Top="128" />
                <wpf:Valve Component="vSplit_GM" Canvas.Left="514" Canvas.Top="128" />
                
            </Canvas>

        </DockPanel>
    </Grid>
</wpf:ControlPanel>
